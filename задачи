include <cmath>
float >> a;
cin >> a;
float x = a, x0 = 0;
float eps = 0.00001;
while( abs(x-x0) > eps ) {
x0 = x;
x = (x + a/x) / 2;


for (int k=100; k<=999; k++) {
int squareK = k*k;
if (squareK % 100 == k)
cout << k << endl;
}

